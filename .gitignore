# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.project
.settings/

# Backup files (created by sed -i.bak, etc.)
*.bak
*.orig

# Test output / coverage (project-level — feature folders themselves are tracked)
/test-results/
/coverage/
/tmp/

# Per-feature gate audit history (optional, generated by docs-09-review-gate)
docs/features/*/.gate-history.md

# Direnv / dotenv (if used)
.envrc
.env
.env.local
